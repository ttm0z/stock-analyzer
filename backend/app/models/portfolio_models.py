# Import optimized Portfolio models from new location
from ...models.portfolio import Portfolio, Position, Transaction, PortfolioSnapshot

# All model definitions are now in the optimized /backend/models/ directory
# This file serves as a compatibility layer for existing imports